import React from 'react';
const a=React.useEffect;
// export a;
/*
import React from 'react';
import { setMenuList } from 'app/slice/layoutSlice';
import LoadSliceData from 'app/components/loader';

export function PageContainer({ data }) {

}

export function loader({ loadProps, Page, ...rest}) {
 React.useEffect(() => setMenuList(loadProps.menuList), []);
  return <LoadSliceData />;
}
*/
